# Examples: Strict Rules

## CLAUDE.md

```markdown
# Critical Rules

- NEVER delete data without confirmation.
- ALWAYS run `npm lint` before reducing code.
- Use `pnpm` instead of `npm`.

# Tech Stack

- Next.js 14 (App Router)
- Prisma (Postgres)
```
